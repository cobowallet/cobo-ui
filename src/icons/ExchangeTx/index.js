import React from 'react';
import { Image } from 'react-native';

function ExchangeTx({ style }) {
  return <Image style={style} source={require('./img/exchange_tx.png')} />;
}

export default ExchangeTx;
