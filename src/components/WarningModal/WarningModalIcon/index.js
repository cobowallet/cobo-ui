import React from 'react';
import { Image } from 'react-native';

function WarningModalIcon(style) {
  return <Image style={style} source={require('./img/warningModal.png')} />;
}

export default WarningModalIcon;
