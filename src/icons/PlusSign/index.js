import React from 'react';
import PropTypes from 'prop-types';
import { Image } from 'react-native';

function PlusSign() {
  return <Image source={require('./img/plus-sign.png')} />;
}

export default PlusSign;
