import React from 'react';
import { Image } from 'react-native';

function AccountTransfer({ style }) {
  return <Image style={style} source={require('./img/transfer.png')} />;
}

export default AccountTransfer;
