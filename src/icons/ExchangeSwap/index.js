import React from 'react';
import { Image } from 'react-native';

function ExchangeSwap({ style }) {
  return <Image style={style} source={require('./img/exchange_swap.png')} />;
}

export default ExchangeSwap;
