import React from 'react';
import { Image } from 'react-native';

function RewardIcon({ style }) {
  return <Image style={style} source={require('./img/financing.png')} />;
}

export default RewardIcon;
