import React from 'react';
import { Image } from 'react-native';

function ExchangeDrawer({ style }) {
  return <Image style={style} source={require('./img/exchange_drawer.png')} />;
}

export default ExchangeDrawer;
