import React from 'react';
import { Image } from 'react-native';

function ExchangeIcon({ style }) {
  return <Image style={style} source={require('./img/exchange_icon.png')} />;
}

export default ExchangeIcon;
